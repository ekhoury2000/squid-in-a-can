squid:
  build: .
  environment:
    DISK_CACHE_SIZE: 500000
    MAX_CACHE_OBJECT: 100000
  net: "host"
  ## Uncomment and update /path/to/cache
  volumes:
  - /var/cache/mysquid3:/var/cache/squid

tproxy:
  build: iptables_docker/.
  net: "host"
  privileged: true
  links:
  - squid
